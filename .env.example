# Server
PORT=8000
BASE_URL=http://localhost:8000

# Admin dashboard password (required for /admin.html)
ADMIN_PASSWORD=change-me-to-a-long-random-password

# Stripe payments (optional - orders are saved as "pending" without it)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Email notifications via SMTP (optional)
# For Gmail: enable 2-Step Verification, then create an App Password.
# SMTP_HOST=smtp.gmail.com, SMTP_PORT=587, SMTP_USER=your@gmail.com, SMTP_PASS=app-password
SMTP_HOST= jkmungtawn@gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# SMS notifications via Twilio (optional)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
